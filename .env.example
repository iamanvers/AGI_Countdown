# Optional free source API keys used only by the GitHub Actions pipeline.
# The web app must not depend on any secret.

EIA_API_KEY=
BLS_API_KEY=
SEMANTIC_SCHOLAR_API_KEY=
GITHUB_TOKEN=

# Optional enhancement, disabled by default.
ENABLE_LLM_ENRICHMENT=false
ANTHROPIC_API_KEY=
